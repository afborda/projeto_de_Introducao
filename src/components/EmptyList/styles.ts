import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
    marginTop: 52,
    marginBottom: 16
  },
  image: {
    height: 56,
    width: 56
  },
  emptyBold: {
    color: "#808080",
    fontSize: 16,
    marginTop: 16,
    fontWeight: "bold",
    textAlign: "center"
  },
  empty: {
    color: "#808080",
    fontSize: 16,
    marginTop: 16,
    textAlign: "center"
  }
});
